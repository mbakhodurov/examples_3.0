version: '3'

vars:
  GO_VERSION: '1.26.0'
  GOLANGCI_LINT_VERSION: 'v2.12.2'
  GCI_VERSION: 'v0.14.0'
  GOFUMPT_VERSION: 'v0.10.0'
  BUF_VERSION: '1.69.0'
  PROTOC_GEN_GO_VERSION: 'v1.36.11'
  PROTOC_GEN_GO_GRPC_VERSION: 'v1.6.1'

  BIN_DIR: '{{.ROOT_DIR}}/bin'
  GOLANGCI_LINT: '{{.BIN_DIR}}/golangci-lint'
  GCI: '{{.BIN_DIR}}/gci'
  GOFUMPT: '{{.BIN_DIR}}/gofumpt'
  BUF: '{{.BIN_DIR}}/buf'
  PROTOC_GEN_GO: '{{.BIN_DIR}}/protoc-gen-go'
  PROTOC_GEN_GO_GRPC: '{{.BIN_DIR}}/protoc-gen-go-grpc'

  KIBANA_URL: 'http://localhost:{{.KIBANA_PORT}}'
  ELASTICSEARCH_URL: 'http://localhost:{{.ELASTICSEARCH_PORT}}'

dotenv: ['.env']

tasks:
  formatters:install:
    desc: "Устанавливает gofumpt и gci"
    cmds:
      - echo '📦 Устанавливаем gofumpt...'
      - GOBIN={{.BIN_DIR}} go install mvdan.cc/gofumpt@{{.GOFUMPT_VERSION}}
      - echo '📦 Устанавливаем gci...'
      - GOBIN={{.BIN_DIR}} go install github.com/daixiang0/gci@{{.GCI_VERSION}}
    status:
      - test -x {{.GOFUMPT}}
      - test -x {{.GCI}}

  golangci-lint:install:
    desc: "Устанавливает golangci-lint"
    cmds:
      - echo '📦 Устанавливаем golangci-lint...'
      - GOBIN={{.BIN_DIR}} go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@{{.GOLANGCI_LINT_VERSION}}
    status:
      - test -x {{.GOLANGCI_LINT}}

  buf:install:
    desc: "Устанавливает buf"
    cmds:
      - echo '📦 Устанавливаем buf...'
      - GOBIN={{.BIN_DIR}} go install github.com/bufbuild/buf/cmd/buf@v{{.BUF_VERSION}}
    status:
      - test -x {{.BUF}}

  proto:install-plugins:
    desc: "Устанавливает protoc-плагины"
    cmds:
      - echo '📦 Устанавливаем protoc-gen-go...'
      - GOBIN={{.BIN_DIR}} go install google.golang.org/protobuf/cmd/protoc-gen-go@{{.PROTOC_GEN_GO_VERSION}}
      - echo '📦 Устанавливаем protoc-gen-go-grpc...'
      - GOBIN={{.BIN_DIR}} go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@{{.PROTOC_GEN_GO_GRPC_VERSION}}
    status:
      - test -x {{.PROTOC_GEN_GO}}
      - test -x {{.PROTOC_GEN_GO_GRPC}}

  setup:
    desc: "Устанавливает все инструменты"
    deps:
      - formatters:install
      - golangci-lint:install
      - buf:install
      - proto:install-plugins

  format:
    desc: "Форматирует код (gofumpt + gci)"
    deps: [ formatters:install ]
    cmds:
      - echo '🧹 Форматируем код через gofumpt...'
      - find . -type f -name '*.go' ! -path '*/mocks/*' ! -path '*/pkg/proto/*' -exec {{.GOFUMPT}} -extra -w {} +
      - echo '📦 Сортируем импорты через gci...'
      - find . -type f -name '*.go' ! -path '*/mocks/*' ! -path '*/pkg/proto/*' -exec {{.GCI}} write -s standard -s default -s "prefix(github.com/olezhek28-courses/microservices-course-examples/)" {} +

  lint:
    desc: "Запускает golangci-lint"
    deps: [ golangci-lint:install ]
    cmds:
      - '{{.GOLANGCI_LINT}} run --allow-parallel-runners ./... --config=.golangci.yml'

  deps:update:
    desc: "Обновляет зависимости"
    cmds:
      - go mod tidy -compat={{.GO_VERSION}}

  proto:format:
    desc: "Форматирует .proto-файлы (buf format)"
    deps: [ buf:install ]
    dir: proto
    cmds:
      - '{{.BUF}} format -w'

  proto:lint:
    desc: "Проверяет .proto-файлы линтером"
    deps: [ buf:install, proto:install-plugins, proto:format ]
    dir: proto
    cmds:
      - '{{.BUF}} lint'

  proto:gen:
    desc: "Генерирует Go-код из .proto"
    deps: [ buf:install, proto:install-plugins, proto:format, proto:lint ]
    dir: proto
    cmds:
      - '{{.BUF}} generate'

  up:
    desc: "Поднять контейнеры"
    cmds:
      - docker network rm microservices-net 2>/dev/null || true
      - docker compose up --build --detach

  down:
    desc: "Остановить и удалить контейнеры"
    cmds:
      - docker compose down --volumes

  es:wait:
    desc: "Ожидает готовности Elasticsearch"
    cmds:
      - echo "⏳ Ожидаем готовности Elasticsearch..."
      - |
        for i in $(seq 1 60); do
          curl -sSf {{.ELASTICSEARCH_URL}}/_cluster/health >/dev/null 2>&1 && break
          sleep 2
        done

  es:create-template:
    desc: "Создает index template для data stream easy-logs (нужен в ECS-режиме экспортера)"
    cmds:
      - echo "📝 Создаем index template easy-logs-template (data stream)..."
      - |
        curl -sS -X PUT {{.ELASTICSEARCH_URL}}/_index_template/easy-logs-template \
          -H "Content-Type: application/json" \
          --data '{"index_patterns":["easy-logs*"],"data_stream":{},"priority":500}'
        echo ""
        echo "✅ Index template easy-logs-template создан"

  es:init:
    desc: "Ожидает Elasticsearch и создает index template для data stream"
    cmds:
      - task: es:wait
      - task: es:create-template

  kibana:wait:
    desc: "Ожидает готовности Kibana"
    cmds:
      - echo "⏳ Ожидаем готовности Kibana..."
      - |
        for i in $(seq 1 60); do
          curl -sSf {{.KIBANA_URL}}/api/status >/dev/null 2>&1 && break
          sleep 2
        done

  kibana:create-view:
    desc: "Создает Data View в Kibana и устанавливает его как default"
    cmds:
      - echo "📝 Создаем Data View easy-logs*..."
      - |
        RESPONSE=$(curl -sS -X POST {{.KIBANA_URL}}/api/data_views/data_view \
          -H "kbn-xsrf: true" \
          -H "Content-Type: application/json" \
          --data '{"data_view":{"title":"easy-logs*","name":"Easy Logs","timeFieldName":"@timestamp","allowNoIndex":true},"override":true}')
        DATA_VIEW_ID=$(echo "$RESPONSE" | jq -r ".data_view.id // empty")
        echo "Data View ID: $DATA_VIEW_ID"
        if [ -n "$DATA_VIEW_ID" ]; then
          curl -sS -X POST {{.KIBANA_URL}}/api/data_views/default \
            -H "kbn-xsrf: true" \
            -H "Content-Type: application/json" \
            -d "{\"data_view_id\": \"$DATA_VIEW_ID\", \"force\": true}"
          echo "✅ Data View создан и установлен как default"
        else
          echo "❌ Не удалось создать Data View"
          exit 1
        fi

  kibana:init:
    desc: "Готовит Elasticsearch (template) и Kibana (Data View) для просмотра логов"
    cmds:
      - task: es:init
      - task: kibana:wait
      - task: kibana:create-view

  run:
    desc: "Запуск gRPC сервера"
    cmds:
      - go run cmd/grpc_server/main.go

  test:api:
    desc: "Запускает интеграционные тесты API"
    cmds:
      - go test -v -count=1 ./tests/...
